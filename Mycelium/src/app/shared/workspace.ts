export interface Workspace {
    members: string[],
    isStarted: boolean,
    _id: string,
    name: string,
    description: string,
    owner: string,
    year: number,
    series: string,
    group: number
}